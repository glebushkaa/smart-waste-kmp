CREATE TABLE rubbish (
    id INTEGER PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    count INTEGER NOT NULL
);

getAllRubbishes: SELECT * FROM rubbish;
insertRubbish: INSERT OR REPLACE INTO rubbish(id,name,count) VALUES(?,?,?);